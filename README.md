# ABA Music Academy (Website)

Next.js application — public site, student portal, teacher dashboard, and admin tools.

## Development

```bash
npm install
npm run dev
```

Set environment variables in `.env.local` (Supabase, Stripe, `NEXT_PUBLIC_SITE_URL`).
