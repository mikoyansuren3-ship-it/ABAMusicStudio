import type React from "react"

export const metadata = {
  title: "Contact | ABA Music Academy",
  description: "Questions about lessons or the studio? Get in touch with ABA Music Academy.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
