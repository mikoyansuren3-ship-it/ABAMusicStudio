import type React from "react"

export const metadata = {
  title: "Inquire About Lessons | ABA Music Academy",
  description: "Tell us about the prospective student and preferred times — we'll match you with the right lesson.",
}

export default function InquireLayout({ children }: { children: React.ReactNode }) {
  return children
}
